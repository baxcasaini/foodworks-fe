// Questo file viene sostituito durante il build di produzione
// IMPORTANTE: Modifica questo URL con l'URL reale del tuo backend prima del deploy
// Per Vercel: puoi usare uno script pre-build per sostituire questo valore
// Per EC2: modifica questo file direttamente
export const environment = {
  production: true,
  // MODIFICA QUESTO URL con l'URL del tuo backend in produzione
  // Esempi:
  // - Railway: 'https://tuo-backend.railway.app/api/v1'
  // - EC2: 'http://tuo-ec2-ip:8000/api/v1' o 'https://tuo-dominio.com/api/v1'
  apiUrl: 'https://your-backend-url.com/api/v1'
};

